const { ethers } = require('ethers');
console.log("ERC20InvalidSpender(address):", ethers.id("ERC20InvalidSpender(address)").slice(0, 10));
console.log("ERC20InvalidApprover(address):", ethers.id("ERC20InvalidApprover(address)").slice(0, 10));
